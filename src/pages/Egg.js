import React from 'react';

const Egg = () => {
    const tstText = 'Samika kinda smells';

    // React.useEffect( () => {
    //     const pictures = fetchAboutData();
    // }
    // );

    return (
        <h1>
            {tstText}
        </h1>
    );
}

export default Egg;
