import React from 'react';

const Egg = (props) => {
    const tstText = 'Samika kinda smells';

    // React.useEffect( () => {
    //     const pictures = fetchAboutData();
    // }
    // );

    return (
        <h1>
            {tstText}
        </h1>
    );
}

export default Egg;
